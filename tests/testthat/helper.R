# Helpers

library(httptest)
path_to_src <- file.path(getwd(), "testdata", fsep = .Platform$file.sep)
avatar_emf <- paste0(path_to_src, "/avatar.emf")
avatar_jpeg <- paste0(path_to_src, "/avatar.jpeg")
